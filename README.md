# prism-pin-assets

Public image host for Prism (Pinterest pin graphics).

Buffer fetches each image at publish time, so files here must never be deleted or moved until their pin has published. Managed by the Prism distribute skill - do not reorganise by hand.
